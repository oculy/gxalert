namespace GxAlert
{
    /// <summary>
    /// This enumeration allows us to keep track of the current state of the connection
    /// </summary>
    public enum ConnectionState
    {
        Neutral,
        Sending,
        Receiving
    }
}
