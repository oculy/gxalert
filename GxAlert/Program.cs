namespace GxAlert
{
    /// <summary>
    /// This is where our program starts...
    /// </summary>
    public class Program
    {
        /// <summary>
        /// Kick off our server (=listener)
        /// </summary>
        /// <param name="args">Arguments for program start</param>
        private static void Main(string[] args)
        {
            Server s = new Server();

            // Reparse.ReparseRawMessages();
        }
    }
}
